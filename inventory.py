# TODO: list_installed_apps()
Write a minimal Python function list_installed_apps() that currently returns
an empty list and add a __main__ guard that prints that list when the file
is run. Include a short docstring.
